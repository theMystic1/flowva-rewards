import AuthCard from "components/auth/auth";

const Login = () => {
  return (
    <AuthCard
      header="Log in to flowva"
      description="Log in to receive personalized recommendations"
    />
  );
};

export default Login;
